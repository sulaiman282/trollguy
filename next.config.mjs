/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // You can add more specific configurations here if needed, like remotePatterns
  },
};

export default nextConfig;
